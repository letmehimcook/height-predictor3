import tkinter as tk
from tkinter import messagebox
import random

def calculate():
    try:
        # ดึงค่าจากช่องกรอก
        f_h = float(entry_father.get())
        m_h = float(entry_mother.get())
        weight = float(entry_weight.get())
        height_cm = float(entry_height_self.get()) # เพิ่มช่องส่วนสูงตัวเองเพื่อหา BMI
        
        # 1. คำนวณส่วนสูงในอนาคต (MPH)
        if gender_var.get() == 1:  # ชาย
            target_h = (f_h + m_h + 13) / 2
        else:  # หญิง
            target_h = (f_h + m_h - 13) / 2

        # 2. คำนวณ BMI ของตัวเองปัจจุบัน
        # สูตร: น้ำหนัก (kg) / [ส่วนสูง (m) ^ 2]
        bmi = weight / ((height_cm / 100) ** 2)
        
        # 3. จัดกลุ่ม BMI และสุ่มคำแนะนำ
        if bmi < 18.5:
            status = "น้ำหนักน้อย (Underweight)"
            color = "#3498db"
            advices = [
                "🥗 เน้นทานโปรตีนและคาร์โบไฮเดรตที่มีประโยชน์เพื่อเพิ่มมวลกล้ามเนื้อ",
                "🍖 ทานมื้อย่อยเพิ่มระหว่างวัน และอย่าลืมดื่มนมแคลเซียมสูง",
                "💪 ออกกำลังกายแบบแรงต้าน (Weight Training) เพื่อสร้างกล้ามเนื้อ"
            ]
        elif 18.5 <= bmi < 23:
            status = "สมส่วน (Normal)"
            color = "#2ecc71"
            advices = [
                "✅ สุขภาพดีเยี่ยม! รักษาการกินอาหารให้ครบ 5 หมู่แบบนี้ต่อไป",
                "🏃 ออกกำลังกายสม่ำเสมอสัปดาห์ละ 150 นาที เพื่อคงสภาพร่างกาย",
                "😴 พักผ่อนให้เพียงพอเพื่อให้ร่างกายซ่อมแซมส่วนที่สึกหรอ"
            ]
        elif 23 <= bmi < 25:
            status = "น้ำหนักเกิน (Overweight)"
            color = "#f1c40f"
            advices = [
                "🍏 ลดของหวานและน้ำอัดลม เพิ่มการทานผักผลไม้ที่มีกากใย",
                "🚶 พยายามเดินให้มากขึ้น หรือเริ่มทำ Cardo เบาๆ วันละ 30 นาที",
                "💧 ดื่มน้ำเปล่าแทนน้ำหวาน จะช่วยให้ระบบเผาผลาญดีขึ้น"
            ]
        else:
            status = "โรคอ้วน (Obese)"
            color = "#e67e22"
            advices = [
                "🚫 หลีกเลี่ยงอาหารทอด ขมันสูง และเบเกอรี่อย่างจริงจัง",
                "🏊 การว่ายน้ำช่วยลดแรงกระแทกที่ข้อต่อได้ดีสำหรับผู้ที่เริ่มลดน้ำหนัก",
                "👨‍⚕️ ควรปรึกษาผู้เชี่ยวชาญด้านโภชนาการเพื่อวางแผนการกิน"
            ]

        # แสดงผลลัพธ์
        label_result.config(text=f"เป้าหมายส่วนสูง: {target_h:.1f} ซม.", fg="#2c3e50")
        label_bmi.config(text=f"BMI ของคุณ: {bmi:.1f} ({status})", fg=color)
        
        # สุ่มและแสดงคำแนะนำ
        selected_advice = random.choice(advices)
        adv_text.config(state='normal')
        adv_text.delete('1.0', tk.END)
        display_text = (
            f"📊 ผลวิเคราะห์สุขภาพ:\n"
            f"----------------------------------\n"
            f"{selected_advice}\n\n"
            f"🏀 เคล็ดลับความสูง: กระโดดเชือกวันละ 15-20 นาที\n"
            f"😴 เคล็ดลับ Growth Hormone: เข้านอนก่อน 22:00 น."
        )
        adv_text.insert(tk.END, display_text)
        adv_text.config(state='disabled')
        
    except ValueError:
        messagebox.showerror("ข้อผิดพลาด", "กรุณากรอกตัวเลขในช่องส่วนสูงและน้ำหนัก")
    except ZeroDivisionError:
        messagebox.showerror("ข้อผิดพลาด", "ส่วนสูงต้องไม่เป็นศูนย์")

# --- สร้างหน้าต่างหลัก ---
root = tk.Tk()
root.title("Health & Height Predictor Pro")
root.geometry("550x850") 
root.configure(bg="#f4f7f6")

main_font = ("Tahoma", 11)
title_font = ("Tahoma", 16, "bold")

tk.Label(root, text="📏", font=("Tahoma", 40), bg="#f4f7f6").pack(pady=5)
tk.Label(root, text="วิเคราะห์ส่วนสูงและสุขภาพ", font=title_font, bg="#f4f7f6", fg="#2e86de").pack()

# เฟรมสำหรับกรอกข้อมูล
input_frame = tk.Frame(root, bg="#f4f7f6")
input_frame.pack(pady=10)

# ช่องกรอกข้อมูล (พ่อ/แม่/ตัวเอง)
fields = [
    ("ส่วนสูงคุณพ่อ (ซม.):", "entry_father"),
    ("ส่วนสูงคุณแม่ (ซม.):", "entry_mother"),
    ("ส่วนสูงของคุณ (ซม.):", "entry_height_self"),
    ("น้ำหนักของคุณ (กก.):", "entry_weight")
]

entries = {}
for i, (label, name) in enumerate(fields):
    tk.Label(input_frame, text=label, font=main_font, bg="#f4f7f6").grid(row=i, column=0, padx=10, pady=5, sticky="e")
    entry = tk.Entry(input_frame, font=main_font, width=12, justify='center')
    entry.grid(row=i, column=1, pady=5)
    entries[name] = entry

entry_father = entries["entry_father"]
entry_mother = entries["entry_mother"]
entry_height_self = entries["entry_height_self"]
entry_weight = entries["entry_weight"]

# เลือกเพศ
gender_var = tk.IntVar(value=1)
gender_frame = tk.Frame(root, bg="#f4f7f6")
gender_frame.pack(pady=5)
tk.Radiobutton(gender_frame, text="ชาย ♂", variable=gender_var, value=1, font=main_font, bg="#f4f7f6").pack(side=tk.LEFT, padx=15)
tk.Radiobutton(gender_frame, text="หญิง ♀", variable=gender_var, value=2, font=main_font, bg="#f4f7f6").pack(side=tk.LEFT, padx=15)

# ปุ่มคำนวณ
btn_calc = tk.Button(root, text="คำนวณและวิเคราะห์", command=calculate, bg="#28a745", fg="white", 
                     font=("Tahoma", 13, "bold"), width=25, cursor="hand2")
btn_calc.pack(pady=15)

# ส่วนแสดงผลลัพธ์
label_result = tk.Label(root, text="", font=("Tahoma", 18, "bold"), bg="#f4f7f6")
label_result.pack()
label_bmi = tk.Label(root, text="", font=("Tahoma", 14, "bold"), bg="#f4f7f6")
label_bmi.pack(pady=5)

# ส่วนคำแนะนำ
adv_text = tk.Text(root, font=("Tahoma", 11), height=10, width=50, bg="#ffffff", 
                   relief="flat", padx=20, pady=20, state='disabled')
adv_text.pack(pady=15)

root.mainloop()